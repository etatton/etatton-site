{
  "name": "etatton-site",
  "private": true,
  "version": "1.0.0",
  "description": "etatton.com — static personal index page. The site itself has no dependencies; these scripts only run the checks.",
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\""
  }
}
