# etatton.com

Ed Tatton's personal index page: the projects, what each is for, and where to find them.
Static Cloudflare Pages site with no build step. See `CLAUDE.md` for the map and rules.

```bash
python3 -m http.server 8787   # preview at http://localhost:8787/
npm test                      # contact-function + markup checks (no dependencies)
```
